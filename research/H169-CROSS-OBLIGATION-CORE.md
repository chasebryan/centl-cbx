# h169 cross-coordinate obligation core

**Status:** exact explanation layer over the landed h169 pair-route grammar, revision 5  
**Date:** 2026-08-17  
**Analyzer:** `analyze_h169_cross_obligation_core.py`  
**Ancestry coordinate:** `t mod11` from the exact h169 k11-miss theorem

## 1. The machine now carries simultaneous arithmetic obligations

Under an inherited h169 k11 combined miss,

```text
t11=t mod11 in {0,2,3,4,8}.
```

The analyzer tensors this exact ancestry coordinate with the landed h169 pair-route dependency grammar.

A state may now carry

```text
phase restrictions
survivor modes
forced rational factors
signed-support shells
support-subgroup normal forms
CRT phase deletions
valuation budgets
persistent local cycles
support-isolation laws
and contradiction cores.
```

The objective is no longer to record a long negative word. It is to accumulate exact arithmetic obligations until either the state dies or the remaining escape mechanism becomes explicit.

---

## 2. The canonical cross-coordinate contradiction

On h169,

```text
T=8+35t.
```

The phase

```text
t11=8
```

gives

```text
T=2 mod11
-> 11|C19.
```

On either realized pair route,

```text
C19=S*R
```

with `S=391` or `1081`, both coprime to11. Hence `11|R`.

But k19 BARE requires every prime divisor of `R` to be `1 mod19`, while `11 mod19=11`. Therefore

```text
t11=8 + k19 BARE
-> contradiction.
```

The analyzer returns the inclusion-minimal four-atom explanation

```text
ASSUMPTION  t11=8
ASSUMPTION  k19_mode=BARE
THEOREM     t11=8 iff factor11_in_R
THEOREM     factor11_in_R excludes BARE.
```

Minimality is relative to the landed h169 dependency grammar held fixed as background proof data.

---

## 3. The same phase contracts the vertical resource

On `t11=8`, any surviving k19 miss must be `FULL_QR`.

The exact seed11 q19 Type-II-miss automaton also gives

```text
Omega_NR(C19)<=2,
```

instead of the generic unseeded h169 local bound `<=8`.

So one early ancestry phase produces both

```text
horizontal deletion: BARE removed
vertical contraction: Omega_NR 8 -> 2.
```

---

## 4. Phase t11=2 imports the canonical k43 seeded shell

The canonical theorem `H169-K11-T2-K43-SEEDED-SHELL.md` proves

```text
t11=2
-> T=1 mod11
-> 11|C43.
```

One forced factor11 preloads exact q43 signed support

```text
{1,4,11}.
```

The complete local Type-II-miss state space contracts

```text
18,048 -> 2,317,
```

and the combined-miss portion contracts

```text
7,740 -> 1,217.
```

The seeded shell also proves the exact one-occurrence absorber set

```text
{32,39,42} mod43.
```

Thus a k43 Type-II miss cannot contain any additional prime-factor occurrence in those classes.

For a combined miss the seed creates a new h169 CRT deletion

```text
t mod43 != 9.
```

The exact nonresidue valuation resource also contracts

```text
Omega_NR(C43): 20 -> 14.
```

---

## 5. Phase t11=0 imports the canonical k51 Jacobi normal form

Write

```text
t=11u.
```

Then

```text
C51=55(1+42u)=55R.
```

The correct hard-class seed is therefore

```text
[5,11].
```

Define

```text
H51={r in U(51):(r/3)(r/17)=+1}.
```

The canonical theorem `H169-K11-T0-K51-JACOBI-NORMAL-FORM.md` proves the exact word-level equivalence

```text
k51 combined miss
iff
every prime-factor occurrence of R lies in H51.
```

The seed `[5,11]` Type-II-miss closure has exactly

```text
86 states
=26 combined misses
+60 Type-I-only states.
```

The augmented word automaton proves

```text
combined misses using any outside-H51 residual factor: 0.
```

So `H51` is not merely a sufficient shield. It is the complete combined-miss residual-support normal form on this phase.

---

## 6. The k51 normal form creates a CRT contraction

For h169 primes, `p=1 mod3`.

A k51 combined miss has `p` in the Jacobi-positive half of `U(51)`, hence `(p/17)=+1`.

This leaves exactly

```text
t mod17 in {0,2,8,10,11,12,15,16}.
```

Among the 16 prime-admissible mod17 phases, exactly one half remain.

Thus the k51 support normal form feeds back into the global CRT phase state.

---

## 7. k51 exposes the exact vertical escape carrier

The companion theorem `H169-K11-T0-K51-PERSISTENT-SHIELD-CYCLE.md` asks whether valuation at the already-forced prime11 can remain inside the k51 combined-miss normal form.

It can, locally.

Starting from `[5,11]`, four additional copies of11 saturate the full subgroup

```text
H51=<11>.
```

Equivalently, total `v11(C51)=5` is enough to saturate the shield support.

After saturation the support remains `H51`, while the center rotates by multiplication by11. Since `ord_51(11)=16`, the exact signed-box state has period16. Every state on that saturated cycle is a combined miss.

Therefore

```text
local k51 residue geometry imposes no finite ceiling on v11(C51).
```

This is a useful negative theorem: it identifies precisely what a global termination proof must break.

---

## 8. The residual shield support is almost completely isolated

Revision 5 adds `H169-K11-T0-K51-RESIDUAL-SUPPORT-ISOLATION.md`.

On `t=11u`, write

```text
R=1+42u.
```

Every other Lane-I companion through k55 has the form

```text
Ck=ak+2310u.
```

Any common divisor of `R` and `Ck` divides

```text
2310R-42Ck=2310-42ak.
```

Because

```text
R=1 mod2,
R=1 mod3,
R=1 mod7,
```

all determinant factors 2,3,7 disappear. The exact result is

```text
gcd(R,C3)=1
gcd(R,C15)=1
gcd(R,C19)=1
gcd(R,C23)=1
gcd(R,C27)=1
gcd(R,C35)=1
gcd(R,C39)=1
gcd(R,C43)=1
gcd(R,C47)=1
gcd(R,C55)=1.
```

Only three small overlap channels survive:

```text
gcd(R,C7)=11   iff u=6 mod11,
gcd(R,C11)=5   iff u=2 mod5,
gcd(R,C31)=5   iff u=2 mod5,
```

with gcd1 otherwise.

Hence

```text
q notin {5,11} and q|R
->
q divides no other Lane-I companion through k55.
```

In particular every prime `q>11` in the k51 residual is private to k51 throughout the early companion block.

This turns the vague idea that consecutive cofactors cannot behave independently into an exact negative-sharing theorem on the surviving shield reservoir.

---

## 9. The exceptional overlaps do not create a fake contradiction

The only shared residual factors are locally admissible under the corresponding landed miss shields:

```text
11 mod7=4 is QR mod7,
5 mod11=5 is QR mod11,
5 mod31=5 lies in H31={1,5,25}.
```

So the support-isolation theorem does not pretend that the small overlaps kill the branch.

Instead it says the hard part of the H51 escape cannot be outsourced to neighboring cofactors. Any large H51-supported prime factor of `R` is a genuinely private obligation of k51.

---

## 10. The next global target is sharper

The local k51 problem is now classified by three exact layers:

```text
combined miss iff residual support lies in H51
+
persistent period-16 v11 escape inside H51
+
large residual support private to C51 through k55.
```

Therefore more local enumeration is not the priority.

The simultaneous survivor system must force

```text
some prime q|R with q notin H51,
```

or equivalently

```text
Jacobi(q/51)=-1.
```

Since such a private factor cannot be obtained by direct sharing with the neighboring cofactors, the promising routes are now

```text
reciprocity,
CRT ancestry,
product-character constraints,
companion identities,
or a simultaneous character law across the consecutive block.
```

A theorem of the form

```text
simultaneous earlier/later misses
-> some q|R has Jacobi(q/51)=-1
```

would immediately puncture the canonical k51 shield and delete the t11=0 child.

---

## 11. Current five-child k11 obligation map

```text
t11=8 -> C19
    factor11 enters R
    k19 BARE deleted
    Omega_NR 8 -> 2

t11=4 -> C35
    factor11 enters the k35 branch
    S7 deleted
    k35 miss becomes J35-only

t11=3 -> C39
    routed J39+ support theorem active

t11=2 -> C43
    forced support {1,4,11}
    Type-II-miss states 18,048 -> 2,317
    combined misses 7,740 -> 1,217
    added residues {32,39,42} forbidden under Type-II miss
    t43=9 deleted under combined miss
    Omega_NR 20 -> 14

t11=0 -> C51
    C51=55R with forced seed [5,11]
    combined miss iff every residual factor lies in H51
    seed55 Type-II-miss states =86
    combined states =26
    t17 restricted to {0,2,8,10,11,12,15,16}
    H51 saturates at total v11=5
    exact persistent cycle period=16
    no local v11 ceiling
    R coprime to ten other companions through k55
    only overlap channels: 11 with C7, 5 with C11/C31.
```

There is no unclassified child in this factor11 calendar.

The frontier is now simultaneous satisfiability of the complete obligation set.

---

## 12. Formal grammar counts remain separate from arithmetic existence

The landed base grammar contains

```text
Route A: 105,600 formal tuples
Route B: 147,900 formal tuples.
```

Tensoring with the five k11 phases and applying the exact `t11=8` BARE deletion leaves

```text
Route A: 516,450
Route B: 736,950.
```

These are formal symbolic grammar counts only.

The k43 and k51 imported theorems are carried as proof-bearing obligation objects because their strongest conclusions concern signed support, factor words, CRT subphases, valuation resources, and support isolation rather than a single Boolean mode bit.

---

## 13. Interface

Self-test:

```sh
python3 research/analyze_h169_cross_obligation_core.py --self-test
```

Canonical contradiction:

```sh
python3 research/analyze_h169_cross_obligation_core.py \
  --route B \
  --state-json '{"t_mod_11":8,"k19_mode":"BARE"}'
```

k43 obligation ledger:

```sh
python3 research/analyze_h169_cross_obligation_core.py \
  --route A \
  --state-json '{"t_mod_11":2}'
```

k51 obligation ledger:

```sh
python3 research/analyze_h169_cross_obligation_core.py \
  --route B \
  --state-json '{"t_mod_11":0}'
```

---

## 14. Claim boundary

The five `t mod11` values are necessary phases under an inherited h169 k11 combined miss. The analyzer does not assert every phase is arithmetically realized after full ancestry.

The k43 and k51 normal forms are exact local residue/multiplicity theorems with exact h169 phase bridges. Their abstract states are not automatically arithmetic realizations.

The persistent k51 factor11 cycle proves only that local k51 geometry cannot impose a finite `v11` ceiling. It does not assert h169 prime corridor candidates realize unbounded `v11`.

The residual-support theorem proves privacy of large k51 residual factors through k55. It does not force a factor outside `H51`.

Formal tuple counts are not prime counts. No finite Lane-I ceiling or Erdős-Straus proof is claimed here.
