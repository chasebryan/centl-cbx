# h169 k11 phase -> k51 composite Jacobi shield

**Status:** exact composite-modulus local theorem  
**Date:** 2026-08-17  
**Scope:** h169 under an inherited k11 combined miss  
**Verifier:** `verify_h169_k11_phase_k51_composite_shield.py`

## 1. The last unresolved factor11 child is different

Write

```text
p = 169 + 840t
T = (p+23)/24 = 8 + 35t.
```

The landed h169 k11 theorem restricts a combined miss to

```text
t mod11 in {0,2,3,4,8}.
```

The remaining untouched child is

```text
t=0 mod11.
```

Then

```text
T=8 mod11
```

and at k51

```text
C51=(p+51)/4
   =6T+7
   =55+210t.
```

Therefore

```text
11|C51.
```

The deterministic factor11 calendar was correct: this phase delivers literal factor11 to k51.

But k51 is not another q43.

The modulus is composite:

```text
51=3*17.
```

So the correct signed-box state space is the unit group

```text
U(51)=(Z/51Z)^*,
```

not a prime-field discrete-log automaton.

---

## 2. Exact U(51) signed-box automaton

The verifier represents a local state by

```text
(C mod51, signed divisor support in U(51)).
```

For a prime-factor residue r in U(51), one occurrence performs

```text
C' = C*r mod51
S' = S union rS union r^(-1)S.
```

The Lane-I targets are

```text
Type II: -1 mod51
Type I : -(4C)^(-1) mod51.
```

Because a prime h169 Lane-I candidate cannot have 3 or17 dividing C51, every actual prime-factor residue of C51 lies in U(51). Thus this is the correct complete local residue universe for the k51 stage.

The unseeded exact Type-II-miss closure contains

```text
3,337 states
= 1,626 combined misses
+ 1,711 Type-I-only states.
```

Preloading the forced literal factor11 contracts it to

```text
636 states
= 268 combined misses
+ 368 Type-I-only states.
```

So the phase removes

```text
2,701
```

abstract local Type-II-miss states.

That is a real contraction.

It is not the important part.

---

## 3. Factor11 generates an exact index-two shield

Modulo51,

```text
ord_51(11)=16.
```

Define

```text
H=<11>.
```

Then

```text
|U(51)|=32
|H|=16
[U(51):H]=2.
```

The verifier checks the stronger identity

```text
H = ker Jacobi(./51).
```

Explicitly,

```text
H={1,4,5,11,13,14,16,19,20,23,25,29,41,43,44,49}.
```

Thus factor11 does not merely survive the composite modulus. It generates the entire Jacobi +1 half of the unit group.

---

## 4. Both Lane-I targets live outside the shield

The Type-II target is

```text
-1 = 50 mod51.
```

It has

```text
Jacobi(-1/51)=-1,
```

so

```text
-1 notin H.
```

For Type I, factor out the dependence on C:

```text
-(4C)^(-1)
=
(-4^(-1))*C^(-1).
```

Modulo51,

```text
-4^(-1)=38
```

and

```text
Jacobi(38/51)=-1,
```

so

```text
38 notin H.
```

If `C in H`, then `C^(-1) in H`, and therefore

```text
38*C^(-1)
```

lies in the opposite coset of H.

Hence:

```text
support subset H
-> Type II absent
and
C in H, support subset H
-> Type I absent.
```

This yields an exact composite shield theorem:

```text
If every prime-factor residue of C51 lies in H,
then k51 is a combined miss.
```

Since

```text
H=ker Jacobi(./51),
```

one may state the same condition as

```text
every prime-factor residue has Jacobi +1 modulo51.
```

---

## 5. The forced factor is inside the shield

The forced factor11 satisfies

```text
11 in H.
```

Therefore the k11 ancestry phase does not puncture the k51 shield.

It feeds it.

This reverses the behavior seen at k19. There, factor11 collided with BARE and deleted a survivor mode. Here, factor11 is a generator of a complete combined-miss support subgroup.

That is an exact example of a directed entanglement whose local sign is obstructive rather than constructive.

---

## 6. Pure factor11 multiplicity survives indefinitely in the local state machine

Start with

```text
C=11^e
```

at the residue-support level.

After e occurrences, the signed support is generated by powers

```text
11^j, -e<=j<=e.
```

At

```text
e=8,
```

the support saturates the full 16-element subgroup H.

From that point forward, adding another factor11 leaves the support equal to H while C rotates through H.

Because

```text
ord_51(11)=16,
```

the exact state returns after 16 more occurrences:

```text
state(e=24)=state(e=8).
```

Every checked state on this orbit is a combined miss, and the subgroup theorem proves that the cycle can be repeated indefinitely inside the local residue grammar.

Therefore:

```text
local k51 geometry supplies no finite ceiling on factor11 multiplicity.
```

This is not a failure of the machinery.

It identifies the escape mechanism exactly.

---

## 7. This is the vertical escape carrier we were looking for

The earlier picture was

```text
bounded character motion -> finite state / SCC
valuation motion          -> possible escape.
```

The k51 child now exhibits that architecture in a particularly clean form.

Horizontal state has closed:

```text
seed11 Type-II-miss closure = 636 states.
```

Yet one SCC contains an indefinitely repeatable arithmetic resource:

```text
11,11,11,...
```

without creating either Lane-I target.

The obstruction is therefore no longer vague language such as

```text
valuation might grow forever.
```

It has a named carrier:

```text
H51=<11>=ker Jacobi(./51).
```

And it has an exact cycle:

```text
support saturation at exponent8
followed by period16.
```

This is the object the simultaneous consecutive-cofactor system now has to kill.

---

## 8. The next question is no longer local k51

A generic local attack on k51 cannot prove termination because the local model contains a genuine persistent combined-miss shield.

The next theorem target is therefore cross-coordinate:

```text
Can the earlier simultaneous survivor obligations force
at least one prime-factor residue of C51 outside H51?
```

Equivalently:

```text
Can the consecutive-cofactor ancestry force
Jacobi(q/51)=-1
for some q|C51?
```

If yes, the shield is punctured.

If not, the state must pay for remaining entirely inside an index-two support subgroup while neighboring cofactors satisfy their own incompatible support laws.

That is exactly where consecutive-cofactor coupling should now be applied.

---

## 9. Status of the five k11 phase children

The factor11 partition is now locally classified across all five children:

```text
t11=8 -> k19
           BARE deleted
           Omega_NR 8 -> 2

t11=4 -> k35
           existing 3-adic branch collapse

t11=3 -> k39
           existing routed support theorem

t11=2 -> k43
           Type-II-miss states 18048 -> 2317
           Omega_NR 20 -> 14

t11=0 -> k51
           Type-II-miss states 3337 -> 636
           persistent Jacobi shield H51=<11>
           no local factor11 multiplicity ceiling.
```

So the five-way k11 factor11 calendar is no longer a list of future destinations. Every child now lands in explicit theorem structure.

The frontier has moved from

```text
Where does factor11 go?
```

to

```text
Can the simultaneous survivor system coexist with all five consequences?
```

---

## 10. Executable verification

Run

```sh
python3 research/verify_h169_k11_phase_k51_composite_shield.py
```

The verifier checks:

```text
the inherited h169 k11 phase set,
t=0 mod11 -> 11|C51,
the deterministic calendar destination k51,
all 32 units modulo51,
the complete unseeded U(51) Type-II-miss closure,
the complete seed11 U(51) Type-II-miss closure,
combined-miss and Type-I-only splits,
ord_51(11)=16,
H=<11> of order16 and index2,
H=ker Jacobi(./51),
-1 outside H,
-4^(-1)=38 outside H,
Type-I target outside H for every C in H,
support saturation after eight factor11 occurrences,
the exact 16-state repeated-factor11 cycle,
and persistence of the combined-miss class on that cycle.
```

---

## 11. Claim boundary

The theorem does not say a k11 miss forces `t=0 mod11`.

It does not assert actual prime corridor candidates realize arbitrary powers of11 in C51. The repeated-factor cycle is an exact local residue-state possibility, not an existence theorem for primes in every valuation class.

It does not say the Jacobi shield survives all earlier and later cofactor obligations. That is now precisely the next question.

No finite Lane-I ceiling or Erdős-Straus proof is claimed by this theorem alone.
